version: 2
updates:
  # The project has no runtime dependency: only the workflow actions need
  # watching, so that no action is left pinned to an outdated version.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    commit-message:
      prefix: ci
